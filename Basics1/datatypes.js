"use strict"; // treat all JS code as newer version

// alert("Hello")
// alert(5+5) // We're using node.js not the browser

console.log(3+3)

console.log("Development")

let flag = true //boolean 
let name = "Shivam" //string
let digit = 50 //number
let blank //undefined
let invalid = null //object

console.table([flag,name,digit,blank,invalid])

console.table([typeof(flag),typeof(name),typeof(digit),typeof(blank),typeof(invalid)])


